.sidebar
  .wrap.tags
    %h4 标签
    - @tags.each do |tag|
      = link_to node_path(tag.id), :class => 'label' do
        = tag.name
  .wrap.authors
    %h4 作者
    - @authors.each do |author|
      = link_to author_path(author.id), :class => 'label' do
        = author.name

