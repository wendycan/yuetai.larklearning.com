#main-content
  .row
    .large-12.columns
      %ul.inline-block.text-right
        %li.button.tiny.random-btn.radius 
          %i.fa.fa-magic.fa-lg
          GO
  .row
    .large-12.columns
      #random-blog
        .loading.text-center
          %i.fa.fa-spinner.fa-spin.fa-3x
  .row
    .large-12.columns
      %ul.inline-block.text-right
        %li.button.tiny.random-btn.radius 
          %i.fa.fa-magic.fa-lg
          GO

%script{type: 'text/template', id: 't-random-blog'}
  %h3 {{= blog.title }}
  / .username 作者: &nbsp; {{= blog.user.username }}
  .date 日期: &nbsp; {{= blog.date }}
  .body {{= blog.body }}
     
= content_for :js_ext do
  :javascript
    $(document).ready(function () {
      var blogs_random_view = new Yuetai.Views.Blogs.RandomView();
    });